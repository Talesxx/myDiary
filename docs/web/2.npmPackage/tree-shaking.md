# 摇树优化（Tree-shaking）

- 摇树优化是指描述 JavaScript 上下文中移除未引用代码（dead code）行为的术语。
- 简单地说，摇树意味着从包中删除无法访问的代码（也称为死代码）

它依赖于 import 和 export 语句，用来检测代码模块是否被导出、导入，且被 JavaScript 文件使用。
在现代 JavaScript 应用程序中，我们将多个 JavaScript 文件打包为单个文件时，会使用模块打包器（如 webpack 或 Rollup）自动删除未引用的代码。这对于准备预发布代码的工作非常重要，这样可以使最终文件具有简洁的结构和最小化大小。

在项目开发中一般了解即可，在包开发中你需要知道下面的内容。


### npm
